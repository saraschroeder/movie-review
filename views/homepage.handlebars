<head> 
{{!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>  --}}
    
  
</head>
{{#each movies as |movie| }}
<div class="row mb-4 project card">
  <div class="col-md-5">
    <h2>
      {{{get_emoji}}}
      <a href="/movie/{{movie.id}}">{{movie.name}}</a>
    </h2>
    <p>
      <span class="dollar-amount">{{format_amount movie.needed_funding}}</span>
     <p>

     Click on the movie title to read a review and leave your own rating!
     </p> 
      
    </p>
    <p>Created by {{movie.user.name}} on {{format_date movie.date_created}}</p>
  </div>
  {{!-- <div class="col-md-7">
    <p class="movie-description">if at all possible we need to update our api to include description here
      {{movie.description}}
    </p>
  </div> --}}
</div>
{{/each}}
